version: 0.2

phases:
  install:
    commands:
      - echo "Install phase"
      - npm install
  build:
    commands:
      - echo "Build phase"
      - npm run buildProduction
artifacts:
  files:
    - '**/*'
  base-directory: 'build' 
 